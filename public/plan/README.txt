Add plan-wide, shared videos here so every user on the same plan sees the SAME banner and avatar animations.

Required filenames:
- legend-banner.webm   (1920x400–1920x600, loop, muted)
- champion-banner.webm (same specs)
- warrior-banner.webm  (same specs)

- legend-avatar.webm   (square 512x512 or 720x720, loop, muted)
- champion-avatar.webm (same specs)
- warrior-avatar.webm  (same specs)

Tips:
- Provide .mp4 variants if you need wider browser support (same names, .mp4 extension).
- Keep files <6MB for fast load. No audio.

