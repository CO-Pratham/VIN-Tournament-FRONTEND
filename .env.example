# Environment Variables for VIN Tournament Frontend

# API Base URL - Railway backend URL
# Default: Railway internal URL (only works if frontend is also on Railway)
# For local development, use: http://localhost:8000
# For Railway production (public), use: https://vin-tournament-backend.railway.app
# For Railway internal (if frontend is also on Railway), use: http://vin-tournament-backend.railway.internal
VITE_API_BASE_URL=http://vin-tournament-backend.railway.internal

# WebSocket Base URL (optional, defaults to API_BASE_URL with ws/wss protocol)
# For local development, use: ws://localhost:8000
# For Railway production (public), use: wss://vin-tournament-backend.railway.app
# For Railway internal (if frontend is also on Railway), use: ws://vin-tournament-backend.railway.internal
VITE_WS_BASE_URL=ws://vin-tournament-backend.railway.internal
